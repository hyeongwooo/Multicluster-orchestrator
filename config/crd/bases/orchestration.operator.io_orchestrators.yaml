---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: orchestrators.orchestration.operator.io
spec:
  group: orchestration.operator.io
  names:
    kind: Orchestrator
    listKind: OrchestratorList
    plural: orchestrators
    shortNames:
    - orch
    singular: orchestrator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ksvcURL
      name: KsvcURL
      type: string
    - jsonPath: .status.eventSourceNodePort
      name: NodePort
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Orchestrator is the Schema for the orchestrators API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrchestratorSpec defines the desired state of Orchestrator.
            properties:
              eventLogic:
                description: Optional high-level condition expression for sensor (e.g.,
                  "A || B").
                type: string
              eventName:
                type: string
              eventSource:
                description: 객체 필드는 pointer 로 두면 완전히 생략 가능
                properties:
                  name:
                    description: minimal info; controller renders full EventSource
                      CR
                    type: string
                  nodePort:
                    format: int32
                    type: integer
                  params:
                    additionalProperties:
                      type: string
                    type: object
                  port:
                    description: Prefer fixed NodePort to avoid read-after-write
                    format: int32
                    type: integer
                  type:
                    default: webhook
                    type: string
                required:
                - name
                type: object
              eventSources:
                items:
                  description: |-
                    EDIT THIS FILE!  THIS IS SCAFFOLDING FOR YOU TO OWN!
                    NOTE: json tags are required.  Any new fields you add must have json tags for the fields to be serialized.
                  properties:
                    name:
                      description: minimal info; controller renders full EventSource
                        CR
                      type: string
                    nodePort:
                      format: int32
                      type: integer
                    params:
                      additionalProperties:
                        type: string
                      type: object
                    port:
                      description: Prefer fixed NodePort to avoid read-after-write
                      format: int32
                      type: integer
                    type:
                      default: webhook
                      type: string
                  required:
                  - name
                  type: object
                type: array
              eventType:
                default: webhook
                type: string
              namespace:
                type: string
              placementPolicy:
                properties:
                  allowedClusters:
                    items:
                      type: string
                    type: array
                  cpuWeight:
                    description: Weights for scoring
                    format: int32
                    type: integer
                  deniedClusters:
                    items:
                      type: string
                    type: array
                  gpuWeight:
                    format: int32
                    type: integer
                  latencyWeight:
                    format: int32
                    type: integer
                  memWeight:
                    format: int32
                    type: integer
                  minGPU:
                    format: int32
                    type: integer
                type: object
              sensor:
                description: SensorSpec defines a simplified DSL for sensors (optional;
                  controller can derive defaults).
                properties:
                  dependsOn:
                    items:
                      type: string
                    type: array
                  logic:
                    type: string
                  name:
                    type: string
                  workflowTemplateName:
                    type: string
                required:
                - name
                - workflowTemplateName
                type: object
              service:
                description: 'Deprecated: prefer `services` when you need multiple
                  kservices.'
                properties:
                  concurrencyTarget:
                    format: int32
                    type: integer
                  domainSuffix:
                    description: 'Optional: if you want to compute URL instead of
                      reading status'
                    type: string
                  env:
                    additionalProperties:
                      type: string
                    type: object
                  image:
                    type: string
                  name:
                    description: ServiceSpec can be used in a list of services.
                    type: string
                required:
                - image
                type: object
              services:
                description: |-
                  One or more Knative Services. When set, the controller should prefer this field.
                  If empty, fallback to single Service for backward-compat.
                items:
                  description: ServiceSpec describes a single Knative Service to be
                    created.
                  properties:
                    concurrencyTarget:
                      format: int32
                      type: integer
                    domainSuffix:
                      description: 'Optional: if you want to compute URL instead of
                        reading status'
                      type: string
                    env:
                      additionalProperties:
                        type: string
                      type: object
                    image:
                      type: string
                    name:
                      description: ServiceSpec can be used in a list of services.
                      type: string
                  required:
                  - image
                  type: object
                type: array
            required:
            - eventName
            type: object
          status:
            description: OrchestratorStatus holds runtime status fields.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              eventSourceNodePort:
                format: int32
                type: integer
              ksvcURL:
                type: string
              lastPlacementTime:
                format: date-time
                type: string
              phase:
                description: OrchestratorStatus defines the observed state of Orchestrator.
                enum:
                - Pending
                - WaitingOnKsvc
                - WaitingOnNodePort
                - Ready
                - Error
                type: string
              selectedClusters:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
